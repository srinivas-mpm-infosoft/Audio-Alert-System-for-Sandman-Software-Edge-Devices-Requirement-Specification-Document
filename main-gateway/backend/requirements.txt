# Gateway Configuration UI backend (flask_backend.py) + Phase 2 services
# (dispatch_service.py, heartbeat_service.py, scheduler_service.py,
#  sop_service.py, mqtt_service.py, events_bus.py)

flask
flask-sqlalchemy
pymysql
bcrypt
requests

# Live Voice Paging (D2) + real-time dashboard/SOP updates — WebSocket
flask-sock
simple-websocket
websocket-client

# MQTT support (optional — degrades gracefully if not installed / no broker
# reachable): mqtt_service.py's node heartbeat/status ingestion, AND
# mqtt_transport.py's alert/audio publishing for any Device configured with
# protocol="mqtt" in Devices & Zones. Requires a broker such as Mosquitto —
# see MQTT_SETUP.md.
paho-mqtt

# edge-services — deploy alongside each other:
#   edge_node.py     -> every RPi4 speaker node (port 5000)
#   tts_server.py    -> CM5 gateway (port 6000) — needs GEMINI_API_KEY + VOICEMAKER_API_KEY env vars
#   alert_poller.py  -> CM5 gateway (port 7000)
#
# System packages also required (not pip-installable):
#   ffmpeg, vlc (cvlc), alsa-utils (aplay)

flask
requests
pymysql

# tts_server.py
google-genai
websocket-client

# edge_node.py + tts_server.py — live voice paging / WS delivery
flask-sock
simple-websocket

# edge_node.py — optional MQTT heartbeat publish AND MQTT alert/audio
# receiving (set MQTT_ZONE_ID + MQTT_BROKER_HOST env vars to enable; degrades
# gracefully without it — HTTP /play keeps working either way).
# tts_server.py — needed only if any Device is configured with protocol="mqtt"
# in the Central Gateway (Devices & Zones); unused (never imported) otherwise.
paho-mqtt
